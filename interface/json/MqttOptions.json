{
	"version": "Last Modified: 2025/08/19 12:49:58",
	"fields": {
		"accessible": {
			"fieldClass": "hideme"
		},
		"server": {
			"label": "{{SERVER_NAME}}:",
			"helper": "{{SERVER_NAME_HELP1}}"
		},
		"port": {
			"label": "{{SERVER_PORT}}:",
			"helper": "{{SERVER_POR_HELP1}}"
		},
		"username": {
			"label": "{{USERNAME}}:",
			"helper": "{{USERNAME_HELP1}}"
		},
		"password": {
			"type": "password",
			"label": "{{PASSWORD}}:",
			"helper": "{{Enter your MQTT server password - if required}}"
		},
		"advanced": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"useTls": {
					"label": "TLS:",
					"rightLabel": "{{TLS_ENABLE}}",
					"helper": "{{TLS_HELP1}}"
				},
				"ipVersion": {
					"label": "{{IP_VERSION}}:",
					"helper": "{{IP_VERSION_HELP}}",
					"type": "select",
                    "sort": false,
					"optionLabels": [
						"{{IP_VERSION_OPT1}}",
						"{{IP_VERSION_OPT2}}",
						"{{IP_VERSION_OPT3}}"
					],
					"removeDefaultNone": true
				},
				"protocolVersion": {
					"label": "{{PROTOCOL_VERSION}}:",
					"helper": "{{PROTOCOL_VERSION_HELP1}}",
					"type": "select",
                    "sort": false,
					"optionLabels": [
						"v3.1.0",
						"v3.1.1",
						"v5.0.0"
					],
					"removeDefaultNone": true
				}
			}
		},
		"dataUpdate": {
			"fields": {
				"enabled": {
					"rightLabel": "{{ON_DATA_UPDATE}}"
				},
				"template": {
					"label": "{{TEMPLATE_FILE}}:",
					"helper": "{{TEMPLATE_FILE_HELP1}}",
					"dependencies": {
						"enabled": true
					}
				}
			}
		},
		"interval": {
			"fields": {
				"enabled": {
					"rightLabel": "Enable messages sent at fixed intervals"
				},
				"template": {
					"label": "{{TEMPLATE_FILE}}:",
					"helper": "{{TEMPLATE_FILE_HELP1}}",
					"dependencies": {
						"enabled": true
					}
				}
			}
		}
	}
}