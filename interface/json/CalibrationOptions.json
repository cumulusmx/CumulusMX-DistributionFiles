{
	"version": "Last Modified: 2025/08/16 20:59:05",
	"definitions": {
		"calibnospike": {
			"fields": {
				"offset": {
					"label": "{{OFFSET}}",
					"helper" : "{{OFFSET_HELPER}}"
				},
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				}
			}
		},
		"calib": {
			"fields": {
				"offset": {
					"label": "{{OFFSET}}",
					"helper" : "{{OFFSET_HELPER}}"
				},
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				},
				"spike": {
					"label": "{{SPIKE}}",
					"helper" : "{{SPIKE_HELPER}}"
				}
			}
		},
		"limits": {
			"fields": {
				"offset": {
					"label": "{{OFFSET}}",
					"helper" : "{{OFFSET_HELPER}}"
				},
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				},
				"spike": {
					"label": "{{SPIKE}}",
					"helper" : "{{SPIKE_HELPER}}"
				},
				"limitmin": {
					"label": "{{LIMIT_MIN}}",
					"helper" : "{{LIMIT_MIN_HELPER}}"
				},
				"limitmax": {
					"label": "{{LIMIT_MAX}}",
					"helper" : "{{LIMIT_MAX_HELPER}}"
				}
			}
		}
	},
	"fields": {
		"accessible": {
			"fieldClass": "hideme"
		},
		"explanation": {
			"type": "textarea",
			"rows": 8,
			"validate": false
		},
		"pressure": {
			"collapsible": true,
			"collapsed": true,
			"$ref": "#/definitions/limits",
			"helpers": [
				"{{PRESS_HELPER1}}",
				"{{PRESS_HELPER2}}"
			]
		},
		"pressureStn": {
			"collapsible": true,
			"collapsed": true,
			"$ref": "#/definitions/calibnospike",
			"helpers": [
				"{{STN_PRESS_HELPER1}}",
				"{{STN_PRESS_HELPER2}}",
				"{{STN_PRESS_HELPER3}}"
			]
		},
		"temp": {
			"collapsible": true,
			"collapsed": true,
			"$ref": "#/definitions/limits"
		},
		"tempin": {
			"collapsible": true,
			"collapsed": true,
			"$ref": "#/definitions/calib"
		},
		"hum": {
			"collapsible": true,
			"collapsed": true,
			"$ref": "#/definitions/calib"
		},
		"humin": {
			"collapsible": true,
			"collapsed": true,
			"$ref": "#/definitions/calib"
		},
		"windspd": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				},
				"spike": {
					"label": "{{SPIKE}}",
					"helper" : "{{SPIKE_HELPER}}"
				}
			}
		},
		"gust": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				},
				"spike": {
					"label": "{{SPIKE}}",
					"helper" : "{{SPIKE_HELPER}}"
				},
				"limitmax": {
					"label": "{{LIMIT_MAX}}",
					"helper" : "{{LIMIT_MAX_HELPER}}"
				}
			}
		},
		"winddir": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"offset": {
					"label": "{{OFFSET}}:",
					"helper" : "{{OFFSET_HELPER}}"
				}
			}
		},
		"rain": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"spikerate": {
					"label": "{{RAIN_RATE_SPIKE}}",
					"helper" : "{{SPIKE_HELPER}}"
				},
				"spikehour": {
					"label": "{{RAIN_HOUR_SPIKE}}",
					"helper" : "{{SPIKE_HELPER}}"
				}
			}
		},
		"solar": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"offset": {
					"label": "{{OFFSET}} (W/m²):",
					"helper" : "{{OFFSET_HELPER}}"
				},
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				}
			}
		},
		"uv": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"offset": {
					"label": "{{OFFSET}}:",
					"helper" : "{{OFFSET_HELPER}}"
				},
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				}
			}
		},
		"dewpt": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"limitmax": {
					"label": "{{LIMIT_MAX}}",
					"helper" : "{{LIMIT_MAX_HELPER}}"
				}
			}
		},
		"wetbulb": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"offset": {
					"label": "{{OFFSET}}",
					"helper" : "{{OFFSET_HELPER}}"
				},
				"multiplier": {
					"label": "{{MULTIPLIER}}:",
					"helper" :"{{MULTIPLIER_HELPER}}"
				},
				"multiplier2": {
					"label": "{{MULTIPLIER2}}:",
					"helper": "{{MULTIPLIER2_HELPER}}"
				}
			}
		},
		"snow": {
			"collapsible": true,
			"collapsed": true,
			"fields": {
				"mininc": {
					"label": "{{MIN_INCREMENT}}",
					"helpers" : [
						"{{MIN_INCREMENT_HELPER1}}",
						"{{MIN_INCREMENT_HELPER2}}"
					]
				},
				"spike": {
					"label": "{{SPIKE}}",
					"helpers" : [
						"{{SNOW_SPIKE_HELPER1}}",
						"{{MIN_INCREMENT_HELPER2}}"
					]
				}
			}
		}
	}
}